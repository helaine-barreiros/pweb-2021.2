import React from "react";
import { Button, Checkbox, Generator, PageLayout, PasswordBox, Slider } from "../../core/components";

export const HomePage = () => {
  return (
    <PageLayout>
      <Generator />
    </PageLayout>
  )
}
